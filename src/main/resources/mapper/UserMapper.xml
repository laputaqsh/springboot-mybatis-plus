<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="qsh.laputa.mapper.UserMapper">
    <select id="selectAll" resultType="qsh.laputa.entity.User">
        select *
        from user ${ew.customSqlSegment}
    </select>

    <select id="selectCustomPage" resultType="qsh.laputa.entity.User">
        select *
        from user ${ew.customSqlSegment}
    </select>
</mapper>